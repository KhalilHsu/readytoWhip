Development-only scripts for local detector debugging.

These scripts may print raw window titles, terminal metadata, process command
arguments, workspace URIs, and local filesystem paths. Review output before
sharing it in screenshots, issues, or public logs.
